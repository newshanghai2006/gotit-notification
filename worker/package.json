{
  "name": "gotit-worker",
  "private": true,
  "scripts": { "dev": "wrangler dev --local --ip 0.0.0.0", "deploy": "wrangler deploy", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@cloudflare/workers-types": "^5.20260918.1", "typescript": "^5.8.3", "wrangler": "^4.135.0" }
}
